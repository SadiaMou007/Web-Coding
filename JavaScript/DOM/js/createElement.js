// create the element
const l = document.createElement("li");
l.innerText = "Success";
// where to add(find parent then append the new element as child)
const pul = document.getElementById("pul");
// append new element
pul.appendChild(l);

// add multiple child together
